source 'https://rubygems.org'

# Specify your gem's dependencies in bidding.gemspec
gemspec


group :test do
	gem 'rspec' # testing framework
	gem 'guard' # used to run tests as files change
	gem 'guard-rspec' # used to configure guard to test rspec specs

end